<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.kuali.rice</groupId>
  <artifactId>rice</artifactId>
  <packaging>pom</packaging>
  <name>Rice</name>
  <inceptionYear>2005</inceptionYear>
  <version>2.7.1-SNAPSHOT</version>
  <description>
    The Kuali Rice effort provides an enterprise class middleware suite of integrated products that allows
    both Kuali and non-Kuali applications
    to be built in an agile fashion, such that developers will be able to
    react to
    end-user business requirements in an efficient and productive manner, so
    that they can produce high
    quality business
    applications.
  </description>
  <url>http://www.kuali.org/rice</url>
  <organization>
    <name>Kuali, Inc.</name>
    <url>https://www.kuali.co/</url>
  </organization>

  <properties>

    <project.encoding>UTF-8</project.encoding>
    <project.build.sourceEncoding>${project.encoding}</project.build.sourceEncoding>

    <project.java.version>8</project.java.version>
    <project.compiler.version>${project.java.version}</project.compiler.version>
    <project.compiler.release>${project.java.version}</project.compiler.release>
    <javadoc.doclint>none</javadoc.doclint>

    <maven.skip.packaging>false</maven.skip.packaging>

    <!-- must provide an explicit pattern - surefire default does not work
      b/c of non-unit test classes that are included in default expression -->
    <maven.surefire.includes>**/*Test.java</maven.surefire.includes>
    <maven.surefire.skip>false</maven.surefire.skip>

    <!-- project dependency versions: -->

    <ant.version>1.10.5</ant.version>
    <antlr.version>2.7.7</antlr.version>
    <!--<aopalliance.version>1.0</aopalliance.version>-->
    <asm.group.string>org.ow2.asm</asm.group.string>
    <asm.version>7.1</asm.version>
    <aspectj.version>1.9.2</aspectj.version>
    <bcprov.version>1.57</bcprov.version>
    <bitronix.version>2.1.4</bitronix.version>
    <bytebuddy.version>1.9.10</bytebuddy.version>
    <closure.version>v20170423</closure.version>
    <cglib.version>3.2.10</cglib.version>
    <commons-beanutils.version>1.9.3</commons-beanutils.version>
    <commons-codec.version>1.10</commons-codec.version>
    <commons-collections.version>3.2.2</commons-collections.version>
    <commons-dbcp.version>1.4</commons-dbcp.version>
    <commons-fileupload.version>1.4</commons-fileupload.version>
    <commons-io.version>2.6</commons-io.version>
    <commons-lang.version>2.6</commons-lang.version>
    <commons-lang3.version>3.8.1</commons-lang3.version>
    <commons-pool.version>1.6</commons-pool.version>
    <commons-digester.version>2.1</commons-digester.version>
    <commons-validator.version>1.6</commons-validator.version>
    <cxf.version>3.3.1</cxf.version>
    <displaytag.version>1.2</displaytag.version>
    <dom4j.version>1.6.1</dom4j.version>
    <dwr.version>3.0.2-RELEASE</dwr.version>
    <eclipselink.version>2.7.5</eclipselink.version>
    <ehcache.version>2.10.5</ehcache.version>
    <error_prone_annotations.version>2.3.3</error_prone_annotations.version>
    <freemarker.version>2.3.21</freemarker.version>
    <lesscss.version>1.3.3</lesscss.version>
    <!--<glassfish-javax-json.version>1.1.4</glassfish-javax-json.version>-->
    <groovy.version>2.5.6</groovy.version>
    <guava.version>27.0.1-jre</guava.version>
    <httpclient.version>4.5.7</httpclient.version>
    <jakarta.persistence.version>2.2.2</jakarta.persistence.version>
    <jaxb-impl.version>2.3.2</jaxb-impl.version>
    <jackson.version>2.9.8</jackson.version>
    <javax.activation.version>1.2.0</javax.activation.version>
    <javax.annotation-api.version>1.3.2</javax.annotation-api.version>
    <javax.inject.version>1</javax.inject.version>
    <javax.jws-api.version>1.1</javax.jws-api.version>
    <javax.persistence-api.version>2.2</javax.persistence-api.version>
    <javax.ws.rs-api.version>2.1.1</javax.ws.rs-api.version>
    <javax.xml.soap-api.version>1.4.0</javax.xml.soap-api.version>
    <jaxb-api.version>2.3.1</jaxb-api.version>
    <jaxws-api.version>2.3.1</jaxws-api.version>
    <jaxen.version>1.1.6</jaxen.version>
    <jdom.version>2.0.2</jdom.version>
    <joda-time.version>2.10.1</joda-time.version>
    <joda-time-jsptags.version>1.1.1</joda-time-jsptags.version>
    <json-api.version>1.1.4</json-api.version>
    <jsp-api.version>2.3.3</jsp-api.version>
    <jsr305.version>3.0.2</jsr305.version>

    <!--
      KULRICE-7262: Bad jstl version in maven repository
      When we are able to bump this version from 1.2, go back to the javax.servlet artifact
    -->
    <jstl.version>1.2</jstl.version>

    <jta.version>1.1</jta.version>
    <junit.version>5.4.0</junit.version>
    <kuali-mvn.version>1.0.2</kuali-mvn.version>
    <kuali-util.version>4.4.16</kuali-util.version>
    <jotm.version>2.1.10-kuali-1</jotm.version>
    <log4j.version>2.11.2</log4j.version>
    <mail.version>1.6.2</mail.version>
    <metrics.version>4.0.5</metrics.version>
    <mockito.version>2.24.5</mockito.version>
    <objenesis.version>3.0.1</objenesis.version>
    <plexus-utils.version>3.1.1</plexus-utils.version>
    <quartz.version>2.3.0</quartz.version>
    <saaj-impl.version>1.5.1</saaj-impl.version>
    <servlet-api.version>3.0.1</servlet-api.version>
    <spring-modules-ojb.version>0.8a</spring-modules-ojb.version>
    <spring.version>4.3.22.RELEASE</spring.version>
    <spring.cloud.version>1.1.3.RELEASE</spring.cloud.version>
    <spring.security.version>4.2.7.RELEASE</spring.security.version>
    <struts.version>1.3.11-kuali-1</struts.version>
    <validation.version>2.0.1.Final</validation.version>
    <velocity.version>1.7</velocity.version>
    <velocity.tools.version>2.0</velocity.tools.version>
    <wss4j.version>2.2.3</wss4j.version>
    <xmlsec.version>2.1.2</xmlsec.version>
    <xmlunit.version>2.6.2</xmlunit.version>
    <xapool.version>1.5.0-patch7</xapool.version>
    <xstream.version>1.4.11.1</xstream.version>
    <yuicompressor.version>2.4.8</yuicompressor.version>

    <error_prone_core.version>2.3.3</error_prone_core.version>
    <go-offline-maven-plugin.version>1.1.0</go-offline-maven-plugin.version>
    <license-maven-plugin.version>1.20</license-maven-plugin.version>
    <plugin.ant.version>${ant.version}</plugin.ant.version>
    <plugin.antrun.version>1.8</plugin.antrun.version>
    <plugin.build-helper.version>3.0.0</plugin.build-helper.version>
    <plugin.buildnumber.version>1.4</plugin.buildnumber.version>
    <plugin.clean.version>3.1.0</plugin.clean.version>
    <plugin.compiler.version>3.8.1</plugin.compiler.version>
    <plugin.dependency.version>3.1.1</plugin.dependency.version>
    <plugin.deploy.version>2.8.2</plugin.deploy.version>
    <plugin.docbkx.version>2.0.17</plugin.docbkx.version>
    <plugin.duplicate-finder-maven-plugin.version>1.3.0</plugin.duplicate-finder-maven-plugin.version>
    <plugin.gmavenplus.version>1.9.0</plugin.gmavenplus.version>
    <plugin.install.version>2.5.2</plugin.install.version>
    <plugin.jar.version>3.1.1</plugin.jar.version>
    <plugin.javadoc.version>3.1.0</plugin.javadoc.version>
    <plugin.jaxb-schemagen.version>1.2</plugin.jaxb-schemagen.version>
    <plugin.release.version>2.5.3</plugin.release.version>
    <plugin.site.version>3.7.1</plugin.site.version>
    <plugin.resources.version>3.1.0</plugin.resources.version>
    <plugin.source.version>3.0.1</plugin.source.version>
    <plugin.surefire.version>2.22.0</plugin.surefire.version>
    <plugin.staticweave.version>1.0.0</plugin.staticweave.version>
    <plugin.war.version>3.2.2</plugin.war.version>
    <plugin.spring-maven>3.1.0</plugin.spring-maven>

    <!-- Connect documentation generation to site generation -->
    <rice.docbook.phase>pre-site</rice.docbook.phase>
    <rice.docbook.phase.html>${rice.docbook.phase}</rice.docbook.phase.html>
    <rice.docbook.phase.pdf>${rice.docbook.phase}</rice.docbook.phase.pdf>
    <rice.docbook.phase.multi>${rice.docbook.phase}</rice.docbook.phase.multi>
    <!-- Where the original documentation source is located -->
    <rice.docbook.src>${basedir}/src/site/docbook</rice.docbook.src>
    <!-- Where the filtered documentation source is located -->
    <rice.docbook.filtered>${basedir}/target/docbook-filtered</rice.docbook.filtered>
    <!-- The directory containing content that gets published to the website -->
    <rice.site.dir>${basedir}/target/site</rice.site.dir>
    <!-- Where the generated documentation (pdf, html) will be generated -->
    <rice.docbook.dest>${rice.site.dir}/reference</rice.docbook.dest>
    <docbook.date.format>MM-dd-yyyy</docbook.date.format>

    <!-- phase for wsdl generation from jaxws annotated interfaces -->
    <rice.java2ws.phase>process-classes</rice.java2ws.phase>

    <!-- Directory wsdl's get generated into -->
    <wsdl.dir>${project.build.directory}/wsdl</wsdl.dir>

    <jdk.source.version>${project.java.version}</jdk.source.version>
    <jdk.target.version>${project.java.version}</jdk.target.version>

    <scm.connection>scm:git:https://github.com/kuali/kc-rice.git</scm.connection>
    <scm.developerConnection>scm:git:https://github.com/kuali/kc-rice.git</scm.developerConnection>
    <scm.url>scm:git:https://github.com/kuali/kc-rice</scm.url>

    <issueManagement.system>github</issueManagement.system>
    <issueManagement.url>https://github.com/kuali/kc-rice/issues</issueManagement.url>

    <!-- Override properties from the parent pom for deploying to Maven repositories -->
    <!-- These are the Kuali repository settings for an official release -->
    <kuali.repo.release.id>kualico.nexus.core.releases</kuali.repo.release.id>
    <kuali.repo.release.name>KualiCo Core Release Repository</kuali.repo.release.name>
    <kuali.repo.release.url>https://nexus.kuali.co/nexus/content/repositories/core-releases-public/</kuali.repo.release.url>
    <!-- These are the Kuali repository settings for snapshot builds -->
    <kuali.repo.snapshot.id>kualico.nexus.core.releases</kuali.repo.snapshot.id>
    <kuali.repo.snapshot.name>KualiCo Core Snapshot Repository</kuali.repo.snapshot.name>
    <kuali.repo.snapshot.url>https://nexus.kuali.co/nexus/content/repositories/core-snapshots-public/</kuali.repo.snapshot.url>

    <!-- set the kuali site url for publishing the documentation -->

  </properties>

  <!-- duplicating distributionManagement from parent pom because site plugin doesn't properly allow overriding for the site url -->
  <distributionManagement>
    <repository>
      <id>${kuali.repo.release.id}</id>
      <name>${kuali.repo.release.name}</name>
      <url>${kuali.repo.release.url}</url>
    </repository>
    <snapshotRepository>
      <id>${kuali.repo.snapshot.id}</id>
      <name>${kuali.repo.snapshot.name}</name>
      <url>${kuali.repo.snapshot.url}</url>
    </snapshotRepository>
  </distributionManagement>

  <profiles>
    <profile>
      <id>license</id>
      <properties>
        <license.name>pre_release</license.name>
        <!-- the resolver needs a uri but the template does not.  Just a regular file path -->
        <license.resolver>file://${maven.multiModuleProjectDirectory}/src/license</license.resolver>
        <license.description.template>${maven.multiModuleProjectDirectory}/src/license/header-description.ftl</license.description.template>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.codehaus.mojo</groupId>
              <artifactId>license-maven-plugin</artifactId>
              <version>${license-maven-plugin.version}</version>
              <executions>
                <execution>
                  <id>update-license-header</id>
                  <goals>
                    <goal>update-file-header</goal>
                  </goals>
                  <phase>process-sources</phase>
                </execution>
              </executions>
              <configuration>
                <licenseName>${license.name}</licenseName>
                <licenseResolver>${license.resolver}</licenseResolver>
                <addJavaLicenseAfterPackage>false</addJavaLicenseAfterPackage>
                <descriptionTemplate>${license.description.template}</descriptionTemplate>
                <canUpdateCopyright>true</canUpdateCopyright>
                <copyrightOwners>Kuali, Inc. - All Rights Reserved</copyrightOwners>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>license-maven-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>enforcer</id>

      <activation>
        <property>
          <name>enforce-project-quality</name>
        </property>
      </activation>

      <properties>
        <extra-enforcer-rules.version>1.1</extra-enforcer-rules.version>
        <maven-enforcer-plugin.version>3.0.0-M2</maven-enforcer-plugin.version>

        <enforcer.require.java.version>${project.java.version}</enforcer.require.java.version>
        <enforcer.max.jdk.version>${project.java.version}</enforcer.max.jdk.version>
        <enforcer.phase>validate</enforcer.phase>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <!--
                      This plugin helps enforce that excluded dependencies stay excluded
                      and don't sneak back in through another dependency (a global ban). Here are
                      a few common situations this plugin helps with:

                      1) multiple implementations of a dependency - you only want to use one implementation
                      (ex: commons-logging vs jcl-over-slf4j, javax.mail vs geronimo-javamail_1.4_spec)
                      2) similar maven artifacts are produced for the same project - you only need to include one
                      (ex: cglib vs cglib-nodep)
                      3) an artifact's group id or artifact id has been renamed - maven considers these different
                      artifacts and you only want to include one
                      (ex: opensymphony:quartz vs org.opensymphony.quartz:quartz vs org.quartz-scheduler:quartz)
                      4) an artifact was added to the container and no longer is needed
                      (ex: jaxb in java 6)

                      This plugin also helps enforce consistent a consistent Java & Maven version are used. This helps the
                      stability of builds across systems.

                      This plugin also enforces that plugins specify a version number. Using meta version tags or not specifing a version
                      on a plugin is a decrecated feature as it leads to build instability.

                      Finally, the DependencyConvergence rule helps ensure that all of our submodules are resolving the same versions
                      of
                      artifacts by making sure that all deps converge. This managed at the dep management section which is critical.
                    -->
            <plugin>
              <artifactId>maven-enforcer-plugin</artifactId>
              <version>${maven-enforcer-plugin.version}</version>
              <executions>
                <execution>
                  <id>enforce-banned-dependencies</id>
                  <goals>
                    <goal>enforce</goal>
                  </goals>
                  <phase>${enforcer.phase}</phase>
                  <configuration>

                    <rules>
                      <requireJavaVersion>
                        <version>${enforcer.require.java.version}</version>
                      </requireJavaVersion>
                      <requireMavenVersion>
                        <version>[3.3,4.0)</version>
                      </requireMavenVersion>

                      <requirePluginVersions>
                        <message>Best Practice is to always define plugin versions!</message>
                        <banLatest>true</banLatest>
                        <banRelease>true</banRelease>
                        <banSnapshots>true</banSnapshots>
                        <phases>clean,install,deploy,release</phases>
                      </requirePluginVersions>

                      <banCircularDependencies />
                      <DependencyConvergence />
                      <reactorModuleConvergence />

                      <enforceBytecodeVersion>
                        <maxJdkVersion>${enforcer.max.jdk.version}</maxJdkVersion>
                      </enforceBytecodeVersion>

                      <bannedDependencies>
                        <excludes>

                          <exclude>org.kuali.commons</exclude>
                          <exclude>org.apache.struts</exclude>

                          <!--
                          excluding all versions because of https://issues.apache.org/jira/browse/COLLECTIONS-580
                          including specific fixed version below
                          -->
                          <exclude>commons-collections:commons-collections</exclude>

                          <!-- using bitronix over xapool and jotm -->
                          <exclude>*:xapool</exclude>
                          <exclude>*:jotm-core</exclude>
                          <exclude>logkit:logkit</exclude>
                          <!-- using the kuali version of ojb -->
                          <exclude>ojb</exclude>

                          <!-- renamed (decrecated) groupids -->
                          <exclude>struts</exclude>
                          <exclude>wss4j</exclude>
                          <exclude>acegisecurity</exclude>
                          <exclude>xstream</exclude>
                          <exclude>uk.ltd.getahead</exclude>
                          <exclude>quartz</exclude>
                          <exclude>jdom</exclude>
                          <exclude>groovy</exclude>
                          <exclude>bouncycastle</exclude>
                          <exclude>org.bouncycastle</exclude>
                          <exclude>org.opensymphony.quartz</exclude>
                          <exclude>opensymphony:quartz</exclude>
                          <exclude>javassist</exclude>
                          <exclude>org.jasig.cas</exclude>
                          <exclude>com.google.code.google-collections</exclude>
                          <exclude>com.google.collections</exclude>
                          <exclude>avalon-framework</exclude>
                          <exclude>hsqldb</exclude>

                          <!-- already including cglib-nodep -->
                          <exclude>cglib:cglib</exclude>

                          <!-- already including the jdk15on version of bouncy castle -->
                          <exclude>org.bouncycastle:bcprov-jdk14</exclude>
                          <exclude>org.bouncycastle:bcprov-jdk15</exclude>
                          <exclude>org.bouncycastle:bcprov-jdk16</exclude>

                          <!-- excluding old jstl implementations which can cause hard-to-detect errors -->
                          <exclude>taglibs:standard</exclude>
                          <exclude>javax.servlet:jstl</exclude>
                          <!-- allowing (including) a specific version below -->

                          <!-- generic apis -->
                          <exclude>org.apache.geronimo.modules</exclude>
                          <exclude>org.apache.geronimo.specs</exclude>
                          <exclude>org.apache.servicemix.specs</exclude>
                          <exclude>geronimo-spec</exclude>
                          <exclude>org.ow2.spec.ee</exclude>

                          <!-- already including com.sun.mail:javax.mail -->
                          <exclude>javax.mail:mail</exclude>
                          <exclude>javamail</exclude>
                          <exclude>geronimo:geronimo-mail</exclude>
                          <exclude>org.apache.geronimo.javamail</exclude>

                          <!-- already including javax.transation -->
                          <exclude>jta</exclude>

                          <!-- activation apis -->
                          <exclude>javax.activation</exclude>
                          <exclude>com.sun.activation</exclude>

                          <!-- metadata apis -->
                          <exclude>javax.jws</exclude>

                          <!-- These are repackaged parser apis - some of these apis are
                            included in jdk. The non-jdk parsers can be added indivually if needed. -->
                          <exclude>xerces</exclude>
                          <exclude>xalan</exclude>
                          <exclude>xml-apis</exclude>

                          <!-- group ids for many sun xml apis -->
                          <exclude>javax.xml</exclude>
                          <exclude>com.sun.xml</exclude>

                          <!-- jaxb apis -->
                          <exclude>javax.xml.bind</exclude>
                          <exclude>com.sun.xml.bind</exclude>

                          <!-- jaxws apis -->
                          <exclude>javax.xml.ws</exclude>
                          <exclude>com.sun.xml.ws</exclude>

                          <!-- jax-rs -->
                          <exclude>javax.ws.rs</exclude>

                          <!-- stax apis -->
                          <exclude>javax.xml.stream</exclude>
                          <exclude>com.sun.xml.stream</exclude>
                          <exclude>stax</exclude>

                          <!-- jaxp apis -->
                          <exclude>javax.xml.parsers</exclude>
                          <exclude>com.sun.xml.parsers</exclude>

                          <!-- saaj apis -->
                          <exclude>javax.xml.soap</exclude>

                          <!-- annotations -->
                          <exclude>javax.annotation</exclude>

                          <exclude>org.apache.commons:commons-beanutils</exclude>

                          <!-- exclude eh cache in general - only allow at a specific scope -->
                          <exclude>net.sf.ehcache</exclude>

                          <!--
                            KULRICE-7262: Bad jstl version in maven repository
                            When we go back to the javax.servlet artifact, we'll need to remove this
                          -->
                          <exclude>javax.servlet:jstl</exclude>

                          <!--legacy app server support-->
                          <exclude>javax.servlet:servlet-api</exclude>
                          <exclude>javax.servlet.jsp.jstl:jstl-api</exclude>
                          <exclude>javax.servlet.jsp:jsp-api</exclude>
                          <exclude>org.glassfish.web:jstl-impl</exclude>

                          <!-- old rice deps -->
                          <exclude>htmlunit:htmlunit</exclude>
                          <exclude>org.apache.derby:derby</exclude>

                          <exclude>commons-collections:commons-collections</exclude>
                          <exclude>commons-fileupload:commons-fileupload</exclude>

                          <!-- old rice deps -->
                          <exclude>htmlunit:htmlunit</exclude>
                          <exclude>org.apache.derby:derby</exclude>

                          <exclude>commons-collections:commons-collections</exclude>
                          <exclude>commons-fileupload:commons-fileupload</exclude>
                          <exclude>org.apache.ws.security:wss4j</exclude>
                          <exclude>asm:asm</exclude>
                          <exclude>org.codehaus.jackson</exclude>

                          <exclude>org.opensaml:opensaml</exclude>
                          <exclude>log4j:log4j</exclude>
                          <exclude>commons-logging:commons-logging</exclude>
                          <exclude>org.slf4j:*</exclude>
                          <exclude>org.apache.ant</exclude>

                          <!-- only for tests -->
                          <exclude>org.codehaus.groovy</exclude>
                        </excludes>

                        <includes>
                          <include>org.bouncycastle:bcprov-jdk15on:${bcprov.version}</include>
                          <include>org.bouncycastle:bcmail-jdk15on:${bcprov.version}</include>
                          <include>org.bouncycastle:bcpkix-jdk15on:${bcprov.version}</include>

                          <!--
                            KULRICE-7262: Bad jstl version in maven repository
                            When we are able to bump this version, go back to the javax.servlet artifact
                          -->
                          <!-- <include>javax.servlet:jstl:${jstl.version}</include> -->
                          <include>org.glassfish.web:jstl-impl:${jstl.version}</include>

                          <!-- log4j2 brings in commons-logging for it's jcl dependency and slf4j-api for it's slf4j dependency, make sure it stays runtime scope and cannot be used directly-->
                          <include>org.slf4j:slf4j-api:*:jar:runtime</include>
                          <include>commons-logging:commons-logging:*:jar:runtime</include>

                          <include>net.sf.ehcache:*:${ehcache.version}:jar:runtime</include>
                          <include>commons-collections:commons-collections:${commons-collections.version}</include>
                          <!-- some versions of commons-fileupload have security exploits... only allow a specific version -->
                          <include>commons-fileupload:commons-fileupload:${commons-fileupload.version}</include>
                          <include>org.apache.struts:*:${struts.version}</include>

                          <include>javax.jws:javax.jws-api:${javax.jws-api.version}</include>
                          <include>javax.xml.soap:javax.xml.soap-api:${javax.xml.soap-api.version}</include>
                          <include>javax.xml.ws:jaxws-api:${jaxws-api.version}</include>
                          <include>com.sun.activation:javax.activation:${javax.activation.version}</include>
                          <include>javax.activation:javax.activation-api:${javax.activation.version}</include>
                          <include>javax.xml.bind:jaxb-api:${jaxb-api.version}</include>
                          <include>javax.annotation:javax.annotation-api:${javax.annotation-api.version}</include>
                          <include>javax.ws.rs:javax.ws.rs-api:${javax.ws.rs-api.version}</include>
                          <include>javax.servlet.jsp.jstl:jstl-api:${jstl.version}</include>
                          <include>org.codehaus.groovy:*:${groovy.version}:*:test</include>
                          <!--groovy now brings in ant-->
                          <include>org.apache.ant:*:*:*:test</include>
                        </includes>
                      </bannedDependencies>
                    </rules>
                    <fail>true</fail>
                  </configuration>
                </execution>
              </executions>
              <dependencies>
                <dependency>
                  <groupId>org.codehaus.mojo</groupId>
                  <artifactId>extra-enforcer-rules</artifactId>
                  <version>${extra-enforcer-rules.version}</version>
                </dependency>
              </dependencies>
            </plugin>
          </plugins>
        </pluginManagement>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>dependency-check</id>

      <activation>
        <property>
          <name>enforce-project-quality</name>
        </property>
      </activation>

      <properties>
        <plugin.dependency-check.version>4.0.2</plugin.dependency-check.version>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.owasp</groupId>
              <artifactId>dependency-check-maven</artifactId>
              <version>${plugin.dependency-check.version}</version>
              <configuration>
                <suppressionFile>vulnerability-suppressions.xml</suppressionFile>
                <failBuildOnCVSS>7</failBuildOnCVSS>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
        <plugins>
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- NOT compatible to JDK 11 -->
    <!--
     uses the java jdk jdeps tool to confirm no internal jdk apis are used.  Some of these apis will be removed in
     later versions of java.
     -->
    <!--<profile>
        <id>jdeps</id>

        <activation>
            <property>
                <name>enforce-project-quality</name>
            </property>
        </activation>

        <properties>
            <maven-jdeps-plugin.version>3.1.1</maven-jdeps-plugin.version>
        </properties>
        <build>
            <pluginManagement>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jdeps-plugin</artifactId>
                        <version>${maven-jdeps-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>ban internal jdk apis</id>
                                <goals>
                                    <goal>jdkinternals</goal>
                                    <goal>test-jdkinternals</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jdeps-plugin</artifactId>
                </plugin>
            </plugins>
        </build>
    </profile>-->

    <!-- enables the error prone compiler for strict compilation -->
    <profile>
      <id>error-prone</id>
      <activation>
        <property>
          <name>enforce-project-quality</name>
        </property>
      </activation>

      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-compiler-plugin</artifactId>
              <version>${plugin.compiler.version}</version>
              <configuration>
                <useIncrementalCompilation>false</useIncrementalCompilation>
                <!--<release>${project.compiler.release}</release>-->
                <compilerVersion>${project.compiler.version}</compilerVersion>
                <showDeprecation>true</showDeprecation>
                <showWarnings>true</showWarnings>
                <compilerArgument>-parameters</compilerArgument>
                <compilerArgs>
                  <arg>-Xlint:all</arg>
                  <arg>-Xlint:-processing</arg>
                  <arg>-XDcompilePolicy=simple</arg>
                  <arg>-Xplugin:ErrorProne</arg>
                </compilerArgs>
                <annotationProcessorPaths>
                  <path>
                    <groupId>com.google.errorprone</groupId>
                    <artifactId>error_prone_core</artifactId>
                    <version>${error_prone_core.version}</version>
                  </path>
                </annotationProcessorPaths>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
  </profiles>

  <build>
    <testResources>
      <!-- NOTE: Because Maven test runs can be spawned in a new JVM, the system
        properties on the initial Maven command line are not necessarily propagated
        to the spawned test run. This means we cannot automatically use System property
        definitions to override configuration parameters. To work around this, we
        have configured the test resources to be pre-filtered by maven prior to running,
        which results in config parameter tokens being replaced by System property
        definitions (because the ${token} syntax happens to be the same. So. If you
        need to introduce a new configuration property that needs to be overridable
        by Bamboo/CI, make sure to set the property in one of the below config files,
        so that it can be pre-emptively replaced by Maven prior to kicking off the
        test run. E.g. <param name="my.new.param">${my.new.param}</param> -->
      <testResource>
        <filtering>true</filtering>
        <directory>${basedir}/src/test/resources/META-INF</directory>
        <includes>
          <include>${project.name}-test-config.xml</include>
          <include>sample-app-test-config.xml</include>
        </includes>
      </testResource>
      <testResource>
        <directory>${basedir}/src/test/resources</directory>
      </testResource>
    </testResources>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>de.qaware.maven</groupId>
          <artifactId>go-offline-maven-plugin</artifactId>
          <version>${go-offline-maven-plugin.version}</version>
          <configuration>
            <dynamicDependencies>
              <DynamicDependency>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${plugin.javadoc.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
              <DynamicDependency>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>${plugin.release.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
              <DynamicDependency>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${plugin.source.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
              <DynamicDependency>
                <groupId>org.apache.maven.surefire</groupId>
                <artifactId>surefire-junit-platform</artifactId>
                <version>${plugin.surefire.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
              <DynamicDependency>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${license-maven-plugin.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
              <DynamicDependency>
                <groupId>com.google.errorprone</groupId>
                <artifactId>error_prone_core</artifactId>
                <version>${error_prone_core.version}</version>
                <repositoryType>PLUGIN</repositoryType>
              </DynamicDependency>
            </dynamicDependencies>
            <downloadSources>true</downloadSources>
            <downloadJavadoc>true</downloadJavadoc>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.gmavenplus</groupId>
          <artifactId>gmavenplus-plugin</artifactId>
          <version>${plugin.gmavenplus.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>addTestSources</goal>
                <goal>compileTests</goal>
              </goals>
            </execution>
          </executions>
          <dependencies>
            <dependency>
              <groupId>org.codehaus.groovy</groupId>
              <artifactId>groovy-all</artifactId>
              <version>${groovy.version}</version>
              <scope>runtime</scope>
              <type>pom</type>
            </dependency>
          </dependencies>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-release-plugin</artifactId>
          <version>${plugin.release.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-site-plugin</artifactId>
          <version>${plugin.site.version}</version>
        </plugin>
        <plugin>
          <groupId>com.agilejava.docbkx</groupId>
          <artifactId>docbkx-maven-plugin</artifactId>
          <version>${plugin.docbkx.version}</version>
          <configuration>
            <sourceDirectory>${rice.docbook.filtered}/books</sourceDirectory>
            <xincludeSupported>true</xincludeSupported>
          </configuration>
          <dependencies>
            <dependency>
              <groupId>org.docbook</groupId>
              <artifactId>docbook-xml</artifactId>
              <version>4.4</version>
              <scope>runtime</scope>
            </dependency>
          </dependencies>
        </plugin>

        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${plugin.compiler.version}</version>
          <configuration>
            <fork>true</fork>
            <meminitial>512</meminitial>
            <maxmem>2048</maxmem>
            <forceJavacCompilerUse>true</forceJavacCompilerUse>
            <source>${project.compiler.version}</source>
            <target>${project.compiler.version}</target>
            <!--<release>${project.compiler.release}</release>-->
            <compilerVersion>${project.compiler.version}</compilerVersion>
            <encoding>${project.encoding}</encoding>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${plugin.dependency.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${plugin.deploy.version}</version>
          <configuration>
            <skip>${maven.skip.packaging}</skip>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>${plugin.install.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${plugin.jar.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-source-plugin</artifactId>
          <version>${plugin.source.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-war-plugin</artifactId>
          <version>${plugin.war.version}</version>
        </plugin>
        <plugin>
          <groupId>com.sun.tools.jxc.maven2</groupId>
          <artifactId>maven-jaxb-schemagen-plugin</artifactId>
          <version>${plugin.jaxb-schemagen.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.cxf</groupId>
          <artifactId>cxf-java2ws-plugin</artifactId>
          <version>${cxf.version}</version>
          <configuration>
            <genWsdl>true</genWsdl>
            <verbose>false</verbose>
            <attachWsdl>false</attachWsdl>
          </configuration>
          <dependencies>
            <dependency>
              <groupId>org.apache.cxf</groupId>
              <artifactId>cxf-tools-common</artifactId>
              <version>${cxf.version}</version>
              <exclusions>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-xjc</artifactId>
                </exclusion>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-core</artifactId>
                </exclusion>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-impl</artifactId>
                </exclusion>
              </exclusions>
            </dependency>
            <dependency>
              <groupId>org.apache.cxf</groupId>
              <artifactId>cxf-tools-java2ws</artifactId>
              <version>${cxf.version}</version>
              <exclusions>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-xjc</artifactId>
                </exclusion>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-core</artifactId>
                </exclusion>
                <exclusion>
                  <groupId>com.sun.xml.bind</groupId>
                  <artifactId>jaxb-impl</artifactId>
                </exclusion>
              </exclusions>
            </dependency>

            <dependency>
              <groupId>javax.jws</groupId>
              <artifactId>javax.jws-api</artifactId>
              <version>${javax.jws-api.version}</version>
            </dependency>
            <dependency>
              <groupId>javax.xml.ws</groupId>
              <artifactId>jaxws-api</artifactId>
              <version>${jaxws-api.version}</version>
            </dependency>
            <dependency>
              <groupId>javax.xml.ws</groupId>
              <artifactId>jaxws-api</artifactId>
              <version>${jaxws-api.version}</version>
            </dependency>
          </dependencies>
        </plugin>

        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${plugin.surefire.version}</version>
          <configuration>
            <skip>${maven.surefire.skip}</skip>
            <useFile>false</useFile>
            <failIfNoTests>false</failIfNoTests>
            <includes>
              <include>${maven.surefire.includes}</include>
            </includes>
            <reportFormat>plain</reportFormat>
          </configuration>
        </plugin>
        <plugin>
          <groupId>de.empulse.eclipselink</groupId>
          <artifactId>staticweave-maven-plugin</artifactId>
          <version>${plugin.staticweave.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>weave</goal>
              </goals>
              <phase>process-classes</phase>
              <configuration>
                <logLevel>FINER</logLevel>
                <persistenceXMLLocation>META-INF/persistence-weaving.xml</persistenceXMLLocation>
              </configuration>
            </execution>
          </executions>
          <dependencies>
            <dependency>
              <groupId>org.eclipse.persistence</groupId>
              <artifactId>eclipselink</artifactId>
              <version>${eclipselink.version}</version>
              <exclusions>
                <exclusion>
                  <!-- The JPA API imported separately due to problems with JAR signature. -->
                  <!-- https://stackoverflow.com/questions/46474429/eclipselink-2-7-0-static-weaving-error-for-javax-persistence-attributeconverte -->
                  <groupId>org.eclipse.persistence</groupId>
                  <artifactId>jakarta.persistence</artifactId>
                </exclusion>
              </exclusions>
            </dependency>
            <dependency>
              <groupId>javax.persistence</groupId>
              <artifactId>javax.persistence-api</artifactId>
              <version>${javax.persistence-api.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${plugin.javadoc.version}</version>
          <configuration>
            <doclint>${javadoc.doclint}</doclint>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>buildnumber-maven-plugin</artifactId>
          <version>${plugin.buildnumber.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${plugin.resources.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-antrun-plugin</artifactId>
          <version>${plugin.antrun.version}</version>
          <dependencies>
            <dependency>
              <groupId>org.apache.ant</groupId>
              <artifactId>ant</artifactId>
              <version>${plugin.ant.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>${plugin.build-helper.version}</version>
        </plugin>
        <plugin>
          <groupId>org.kuali.maven.plugins</groupId>
          <artifactId>spring-maven-plugin</artifactId>
          <version>${plugin.spring-maven}</version>
        </plugin>
      </plugins>

    </pluginManagement>

    <plugins>

      <plugin>
        <groupId>org.codehaus.gmavenplus</groupId>
        <artifactId>gmavenplus-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>create-timestamp</id>
            <phase>${rice.docbook.phase}</phase>
            <goals>
              <goal>create-timestamp</goal>
            </goals>
            <configuration>
              <timestampFormat>${docbook.date.format}</timestampFormat>
              <timestampPropertyName>docbook.build.timestamp</timestampPropertyName>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>filter-documentation-source</id>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <phase>${rice.docbook.phase}</phase>
            <configuration>
              <outputDirectory>${rice.docbook.filtered}</outputDirectory>
              <resources>
                <resource>
                  <directory>${rice.docbook.src}</directory>
                  <filtering>false</filtering>
                  <includes>
                    <include>**/*.xml</include>
                    <include>**/*.css</include>
                    <include>**/*.xsl</include>
                  </includes>
                </resource>
                <resource>
                  <directory>${rice.docbook.src}</directory>
                  <filtering>false</filtering>
                  <excludes>
                    <exclude>**/*.xml</exclude>
                    <exclude>**/*.css</exclude>
                    <exclude>**/*.xsl</exclude>
                  </excludes>
                </resource>
              </resources>
            </configuration>
          </execution>
          <execution>
            <id>copy-documentation-resources</id>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <phase>${rice.docbook.phase}</phase>
            <configuration>
              <outputDirectory>${rice.docbook.dest}</outputDirectory>
              <resources>
                <resource>
                  <directory>${rice.docbook.filtered}</directory>
                  <filtering>false</filtering>
                  <excludes>
                    <exclude>**/*.xml</exclude>
                    <exclude>**/*.xsl</exclude>
                  </excludes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.agilejava.docbkx</groupId>
        <artifactId>docbkx-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>generate-html-docs</id>
            <goals>
              <goal>generate-html</goal>
            </goals>
            <phase>${rice.docbook.phase.html}</phase>
            <configuration>
              <htmlStylesheet>../css/stylesheet.css</htmlStylesheet>
              <targetDirectory>${rice.docbook.dest}/html</targetDirectory>
              <tableBordersWithCss>true</tableBordersWithCss>
              <calloutGraphicsPath>../images/callouts/</calloutGraphicsPath>
              <useExtensions>1</useExtensions>
              <linenumberingExtension>1</linenumberingExtension>
              <linenumberingEveryNth>1</linenumberingEveryNth>
            </configuration>
          </execution>
          <execution>
            <id>generate-pdf-docs</id>
            <goals>
              <goal>generate-pdf</goal>
            </goals>
            <phase>${rice.docbook.phase.pdf}</phase>
            <configuration>
              <foCustomization>${rice.docbook.filtered}/stylesheets/fo-stylesheet.xsl</foCustomization>
              <targetDirectory>${rice.docbook.dest}/pdf</targetDirectory>
              <useExtensions>1</useExtensions>
              <linenumberingExtension>1</linenumberingExtension>
              <linenumberingEveryNth>1</linenumberingEveryNth>
            </configuration>
          </execution>
          <execution>
            <id>generate-multi-page-html-docs</id>
            <goals>
              <goal>generate-html</goal>
            </goals>
            <phase>${rice.docbook.phase.multi}</phase>
            <configuration>
              <chunkedOutput>true</chunkedOutput>
              <htmlStylesheet>../../css/stylesheet.css</htmlStylesheet>
              <targetDirectory>${rice.docbook.dest}/html-multi</targetDirectory>
              <calloutGraphicsPath>../../images/callouts/</calloutGraphicsPath>
              <imgSrcPath>../</imgSrcPath>
              <tableBordersWithCss>true</tableBordersWithCss>
              <useExtensions>1</useExtensions>
              <linenumberingExtension>1</linenumberingExtension>
              <linenumberingEveryNth>1</linenumberingEveryNth>
            </configuration>
          </execution>
        </executions>
      </plugin>


      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.basepom.maven</groupId>
        <artifactId>duplicate-finder-maven-plugin</artifactId>
        <version>${plugin.duplicate-finder-maven-plugin.version}</version>
        <executions>
          <execution>
            <id>default</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <failBuildInCaseOfConflict>true</failBuildInCaseOfConflict>
          <ignoredDependencies>
            <dependency>
              <groupId>xmlpull</groupId>
              <artifactId>xmlpull</artifactId>
            </dependency>
            <dependency>
              <groupId>com.yahoo.platform.yui</groupId>
              <artifactId>yuicompressor</artifactId>
            </dependency>
            <dependency>
              <groupId>org.aspectj</groupId>
              <artifactId>aspectjweaver</artifactId>
            </dependency>
            <dependency>
              <groupId>javax.activation</groupId>
              <artifactId>javax.activation-api</artifactId>
            </dependency>
            <dependency>
              <groupId>org.kuali.rice</groupId>
              <artifactId>rice-krad-web-framework</artifactId>
              <classifier>tests</classifier>
            </dependency>
            <dependency>
              <groupId>org.kuali.rice</groupId>
              <artifactId>rice-it-krad</artifactId>
              <classifier>tests</classifier>
            </dependency>
            <dependency>
              <groupId>org.kuali.rice</groupId>
              <artifactId>rice-krad-data</artifactId>
              <classifier>tests</classifier>
            </dependency>
          </ignoredDependencies>
          <ignoredResourcePatterns>
            <ignoredResourcePattern>(.+?)(\\.dtd)?</ignoredResourcePattern>
            <ignoredResourcePattern>(.+?)(\\.xsd)?</ignoredResourcePattern>
            <ignoredResourcePattern>.html</ignoredResourcePattern>
            <ignoredResourcePattern>.txt</ignoredResourcePattern>
          </ignoredResourcePatterns>
          <ignoredClassPatterns>
            <ignoredClassPattern>.*module-info$</ignoredClassPattern>
          </ignoredClassPatterns>
        </configuration>
      </plugin>

    </plugins>

  </build>

  <modules>
    <module>rice-middleware</module>
    <module>rice-framework</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>javax.annotation</groupId>
        <artifactId>javax.annotation-api</artifactId>
        <version>${javax.annotation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.xml.bind</groupId>
        <artifactId>jaxb-api</artifactId>
        <version>${jaxb-api.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.jws</groupId>
        <artifactId>javax.jws-api</artifactId>
        <version>${javax.jws-api.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.xml.soap</groupId>
        <artifactId>javax.xml.soap-api</artifactId>
        <version>${javax.xml.soap-api.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.xml.ws</groupId>
        <artifactId>jaxws-api</artifactId>
        <version>${jaxws-api.version}</version>
      </dependency>

      <dependency>
        <groupId>antlr</groupId>
        <artifactId>antlr</artifactId>
        <version>${antlr.version}</version>
      </dependency>

      <!--<dependency>-->
        <!--<groupId>aopalliance</groupId>-->
        <!--<artifactId>aopalliance</artifactId>-->
        <!--<version>${aopalliance.version}</version>-->
      <!--</dependency>-->

      <!-- explicitly including asm to change version number from what maven would have resolved it to -->
      <dependency>
        <groupId>${asm.group.string}</groupId>
        <artifactId>asm</artifactId>
        <version>${asm.version}</version>
      </dependency>

      <dependency>
        <groupId>cglib</groupId>
        <!-- using nodep to resolve a problem with the asm dependency -->
        <artifactId>cglib-nodep</artifactId>
        <version>${cglib.version}</version>
      </dependency>

      <dependency>
        <groupId>org.aspectj</groupId>
        <artifactId>aspectjrt</artifactId>
        <version>${aspectj.version}</version>
      </dependency>
      <dependency>
        <groupId>org.aspectj</groupId>
        <artifactId>aspectjweaver</artifactId>
        <version>${aspectj.version}</version>
      </dependency>

      <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>${guava.version}</version>
        <exclusions>
          <exclusion>
            <!-- conflicts with closure compiler -->
            <groupId>com.google.errorprone</groupId>
            <artifactId>error_prone_annotations</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>commons-codec</groupId>
        <artifactId>commons-codec</artifactId>
        <version>${commons-codec.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-collections</groupId>
        <artifactId>commons-collections</artifactId>
        <version>${commons-collections.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-dbcp</groupId>
        <artifactId>commons-dbcp</artifactId>
        <version>${commons-dbcp.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-fileupload</groupId>
        <artifactId>commons-fileupload</artifactId>
        <version>${commons-fileupload.version}</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.httpcomponents</groupId>
        <artifactId>httpclient</artifactId>
        <version>${httpclient.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons-io.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>${commons-lang.version}</version>
      </dependency>

      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-lang3</artifactId>
        <version>${commons-lang3.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-pool</groupId>
        <artifactId>commons-pool</artifactId>
        <version>${commons-pool.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-digester</groupId>
        <artifactId>commons-digester</artifactId>
        <version>${commons-digester.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
          <exclusion>
            <artifactId>commons-beanutils</artifactId>
            <groupId>commons-beanutils</groupId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>commons-validator</groupId>
        <artifactId>commons-validator</artifactId>
        <version>${commons-validator.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
          <!-- excluding because tiles requires a newer version than is brought in from 1.3.1 of commons-validator -->
          <exclusion>
            <groupId>commons-digester</groupId>
            <artifactId>commons-digester</artifactId>
          </exclusion>

          <exclusion>
            <artifactId>commons-beanutils</artifactId>
            <groupId>commons-beanutils</groupId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>com.thoughtworks.xstream</groupId>
        <artifactId>xstream</artifactId>
        <version>${xstream.version}</version>
      </dependency>

      <dependency>
        <groupId>displaytag</groupId>
        <artifactId>displaytag</artifactId>
        <version>${displaytag.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl104-over-slf4j</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
          </exclusion>
          <exclusion>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
          </exclusion>
          <exclusion>
            <artifactId>commons-beanutils</artifactId>
            <groupId>commons-beanutils</groupId>
          </exclusion>
          <exclusion>
            <groupId>com.lowagie</groupId>
            <artifactId>itext</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>dom4j</groupId>
        <artifactId>dom4j</artifactId>
        <version>${dom4j.version}</version>
      </dependency>

      <dependency>
        <groupId>com.sun.activation</groupId>
        <artifactId>javax.activation</artifactId>
        <version>${javax.activation.version}</version>
      </dependency>

      <dependency>
        <groupId>com.sun.mail</groupId>
        <artifactId>javax.mail</artifactId>
        <version>${mail.version}</version>
        <exclusions>
          <exclusion>
            <groupId>javax.activation</groupId>
            <artifactId>activation</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>com.sun.xml.messaging.saaj</groupId>
        <artifactId>saaj-impl</artifactId>
        <version>${saaj-impl.version}</version>
        <exclusions>
          <exclusion>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jakarta.activation</groupId>
            <artifactId>jakarta.activation-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jakarta.xml.soap</groupId>
            <artifactId>jakarta.xml.soap-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <!--<dependency>-->
        <!--<groupId>org.glassfish</groupId>-->
        <!--<artifactId>javax.json</artifactId>-->
        <!--<version>${glassfish-javax-json.version}</version>-->
      <!--</dependency>-->

      <dependency>
        <!--
          KULRICE-7262: Bad jstl version in maven repository
          When we are able to bump this version, go back to the javax.servlet artifact.

          Note that the rice-core-impl, rice-core-service-impl, and rice-impl poms also need
          to have this changed. Search on the Jira tag to easily find all the changes.
        -->
        <!-- <groupId>javax.servlet</groupId>
          <artifactId>jstl</artifactId> -->
        <groupId>org.glassfish.web</groupId>
        <artifactId>jstl-impl</artifactId>
        <version>${jstl.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>servlet-api</artifactId>
            <groupId>javax.servlet</groupId>
          </exclusion>
          <exclusion>
            <artifactId>jsp-api</artifactId>
            <groupId>javax.servlet.jsp</groupId>
          </exclusion>
        </exclusions>
        <!-- should be provided scope on newer containers like tomcat 7 -->
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>javax.json</groupId>
        <artifactId>javax.json-api</artifactId>
        <version>${json-api.version}</version>
      </dependency>

      <dependency>
        <groupId>javax.servlet</groupId>
        <artifactId>javax.servlet-api</artifactId>
        <version>${servlet-api.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>javax.servlet.jsp</groupId>
        <artifactId>javax.servlet.jsp-api</artifactId>
        <version>${jsp-api.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>javax.transaction</groupId>
        <artifactId>jta</artifactId>
        <version>${jta.version}</version>
      </dependency>

      <dependency>
        <groupId>javax.ws.rs</groupId>
        <artifactId>javax.ws.rs-api</artifactId>
        <version>${javax.ws.rs-api.version}</version>
      </dependency>

      <dependency>
        <groupId>javax.validation</groupId>
        <artifactId>validation-api</artifactId>
        <version>${validation.version}</version>
      </dependency>


      <!-- explicitly including jaxen to change version number from what maven would have resolved it to -->
      <dependency>
        <groupId>jaxen</groupId>
        <artifactId>jaxen</artifactId>
        <version>${jaxen.version}</version>
      </dependency>

      <dependency>
        <groupId>joda-time</groupId>
        <artifactId>joda-time</artifactId>
        <version>${joda-time.version}</version>
      </dependency>

      <dependency>
        <groupId>joda-time</groupId>
        <artifactId>joda-time-jsptags</artifactId>
        <version>${joda-time-jsptags.version}</version>
      </dependency>
      <dependency>
        <groupId>org.kuali.jotm</groupId>
        <artifactId>jotm-core</artifactId>
        <version>${jotm.version}</version>
        <exclusions>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.ow2.spec.ee</groupId>
            <artifactId>ow2-jta-1.1-spec</artifactId>
          </exclusion>
          <exclusion>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>avalon-framework</groupId>
            <artifactId>avalon-framework</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.objectweb.howl</groupId>
            <artifactId>howl</artifactId>
          </exclusion>
          <exclusion>
            <artifactId>commons-collections</artifactId>
            <groupId>commons-collections</groupId>
          </exclusion>
          <exclusion>
            <artifactId>log4j</artifactId>
            <groupId>log4j</groupId>
          </exclusion>
          <exclusion>
            <groupId>org.jacorb</groupId>
            <artifactId>jacorb</artifactId>
          </exclusion>
          <!--<exclusion>-->
            <!--<groupId>org.jacorb</groupId>-->
            <!--<artifactId>jacorb</artifactId>-->
          <!--</exclusion>-->
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.glassfish.corba</groupId>
        <artifactId>glassfish-corba-orb</artifactId>
        <version>4.2.1</version>
        <exclusions>
          <exclusion>
            <groupId>jakarta.transaction</groupId>
            <artifactId>jakarta.transaction-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.junit.vintage</groupId>
        <artifactId>junit-vintage-engine</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-api</artifactId>
        <version>${log4j.version}</version>
        <scope>compile</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-core</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-web</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-1.2-api</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-jcl</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
        <exclusions>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-slf4j-impl</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-jul</artifactId>
        <version>${log4j.version}</version>
        <scope>runtime</scope>
      </dependency>

      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.objenesis</groupId>
            <artifactId>objenesis</artifactId>
          </exclusion>
          <exclusion>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
          </exclusion>
          <exclusion>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy-agent</artifactId>
          </exclusion>
        </exclusions>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-junit-jupiter</artifactId>
        <version>${mockito.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
          </exclusion>
        </exclusions>
        <scope>test</scope>
      </dependency>

      <!-- updating the version mockito uses -->
      <dependency>
        <groupId>org.objenesis</groupId>
        <artifactId>objenesis</artifactId>
        <version>${objenesis.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- updating the version mockito uses -->
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy</artifactId>
        <version>${bytebuddy.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- updating the version mockito uses -->
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy-agent</artifactId>
        <version>${bytebuddy.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>net.sf.ehcache</groupId>
        <artifactId>ehcache</artifactId>
        <version>${ehcache.version}</version>
        <type>pom</type>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
        </exclusions>
        <!-- consider making this optional so it's not always shipped with rice -->
        <scope>runtime</scope>
      </dependency>

      <dependency>
        <groupId>org.glassfish.jaxb</groupId>
        <artifactId>jaxb-runtime</artifactId>
        <version>${jaxb-impl.version}</version>
        <scope>runtime</scope>
        <exclusions>
          <exclusion>
            <groupId>jakarta.activation</groupId>
            <artifactId>jakarta.activation-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-frontend-jaxrs</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-javamail_1.4_spec</artifactId>
          </exclusion>
          <exclusion>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
          </exclusion>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-core</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <exclusion>
            <groupId>asm</groupId>
            <artifactId>asm</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-javamail_1.4_spec</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.sum.xml.bind</groupId>
            <artifactId>jaxb-core</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.sum.xml.bind</groupId>
            <artifactId>jaxb-impl</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
          </exclusion>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-databinding-aegis</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
      </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-databinding-jaxb</artifactId>
        <version>${cxf.version}</version>
        <scope>runtime</scope>
        <exclusions>
          <exclusion>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-javamail_1.4_spec</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.sun.xml.bind</groupId>
            <artifactId>jaxb-xjc</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.sun.xml.bind</groupId>
            <artifactId>jaxb-impl</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.sun.xml.bind</groupId>
            <artifactId>jaxb-core</artifactId>
          </exclusion>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-bindings-soap</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-frontend-jaxws</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-javamail_1.4_spec</artifactId>
          </exclusion>
          <exclusion>
            <artifactId>asm</artifactId>
            <groupId>asm</groupId>
          </exclusion>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-frontend-simple</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-transports-http</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-rs-client</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <!--<exclusion>-->
            <!--<groupId>com.sun.activation</groupId>-->
            <!--<artifactId>javax.activation</artifactId>-->
          <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-ws-security</artifactId>
        <version>${cxf.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk15</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.wss4j</groupId>
            <artifactId>wss4j-ws-security-dom</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.wss4j</groupId>
            <artifactId>wss4j-ws-security-stax</artifactId>
          </exclusion>
            <!--<exclusion>-->
              <!--<groupId>com.sun.activation</groupId>-->
              <!--<artifactId>javax.activation</artifactId>-->
            <!--</exclusion>-->
          <exclusion>
            <groupId>org.jboss.spec.javax.rmi</groupId>
            <artifactId>jboss-rmi-api_1.0_spec</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <!-- to resolve dependency convergence between opensaml & xmlsec -->
      <dependency>
        <groupId>org.apache.santuario</groupId>
        <artifactId>xmlsec</artifactId>
        <version>${xmlsec.version}</version>
        <exclusions>
          <exclusion>
            <groupId>javax.xml.stream</groupId>
            <artifactId>stax-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.struts</groupId>
        <artifactId>struts-core</artifactId>
        <version>${struts.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
          <exclusion>
            <artifactId>antlr</artifactId>
            <groupId>antlr</groupId>
          </exclusion>
          <exclusion>
            <artifactId>commons-beanutils</artifactId>
            <groupId>commons-beanutils</groupId>
          </exclusion>
          <exclusion>
            <groupId>commons-digester</groupId>
            <artifactId>commons-digester</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.struts</groupId>
        <artifactId>struts-el</artifactId>
        <version>${struts.version}</version>
        <exclusions>
          <exclusion>
            <groupId>taglibs</groupId>
            <artifactId>standard</artifactId>
          </exclusion>
          <exclusion>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.struts</groupId>
        <artifactId>struts-extras</artifactId>
        <version>${struts.version}</version>
      </dependency>

      <dependency>
        <groupId>org.apache.struts</groupId>
        <artifactId>struts-taglib</artifactId>
        <version>${struts.version}</version>
      </dependency>

      <dependency>
        <groupId>org.apache.wss4j</groupId>
        <artifactId>wss4j-bindings</artifactId>
        <version>${wss4j.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId> org.apache.wss4j</groupId>
        <artifactId>wss4j-policy</artifactId>
        <version>${wss4j.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.codehaus.woodstox</groupId>
            <artifactId>woodstox-core-asl</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId> org.apache.wss4j</groupId>
        <artifactId>wss4j-ws-security-common</artifactId>
        <version>${wss4j.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.geronimo.javamail</groupId>
            <artifactId>geronimo-javamail_1.4_mail</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId> org.apache.wss4j</groupId>
        <artifactId>wss4j-ws-security-stax</artifactId>
        <version>${wss4j.version}</version>
      </dependency>
      <dependency>
        <groupId> org.apache.wss4j</groupId>
        <artifactId>wss4j-ws-security-policy-stax</artifactId>
        <version>${wss4j.version}</version>
      </dependency>
      <dependency>
        <groupId> org.apache.wss4j</groupId>
        <artifactId>wss4j-ws-security-dom</artifactId>
        <version>${wss4j.version}</version>
      </dependency>

      <dependency>
        <groupId>org.bouncycastle</groupId>
        <artifactId>bcprov-jdk15on</artifactId>
        <version>${bcprov.version}</version>
      </dependency>

      <dependency>
        <groupId>org.codehaus.groovy</groupId>
        <artifactId>groovy-all</artifactId>
        <version>${groovy.version}</version>
        <scope>test</scope>
        <type>pom</type>
        <exclusions>
          <exclusion>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-swing</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-testng</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-engine</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.codehaus.groovy</groupId>
        <artifactId>groovy</artifactId>
        <version>2.5.6</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-annotations</artifactId>
        <version>${jackson.version}</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>${jackson.version}</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.9.10.1</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.datatype</groupId>
        <artifactId>jackson-datatype-joda</artifactId>
        <version>${jackson.version}</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.jaxrs</groupId>
        <artifactId>jackson-jaxrs-json-provider</artifactId>
        <version>${jackson.version}</version>
        <scope>compile</scope>
      </dependency>

      <dependency>
        <groupId>org.directwebremoting</groupId>
        <artifactId>dwr</artifactId>
        <version>${dwr.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
          <exclusion>
            <groupId>xerces</groupId>
            <artifactId>xmlParserAPIs</artifactId>
          </exclusion>
          <exclusion>
            <groupId>jdom</groupId>
            <artifactId>jdom</artifactId>
          </exclusion>
          <exclusion>
            <groupId>xml-apis</groupId>
            <artifactId>xml-apis</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.antlr</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.asm</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.core</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.jpa</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.jpa.jpql</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.moxy</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>org.eclipse.persistence.oracle</artifactId>
        <version>${eclipselink.version}</version>
      </dependency>

      <dependency>
        <groupId>org.eclipse.persistence</groupId>
        <artifactId>jakarta.persistence</artifactId>
        <version>${jakarta.persistence.version}</version>
      </dependency>

      <dependency>
        <groupId>org.jdom</groupId>
        <artifactId>jdom</artifactId>
        <version>${jdom.version}</version>
      </dependency>

      <!--<dependency>-->
        <!--<groupId>org.kuali.common</groupId>-->
        <!--<artifactId>kuali-mvn</artifactId>-->
        <!--<version>${kuali-mvn.version}</version>-->
        <!--<exclusions>-->
          <!--<exclusion>-->
            <!--<groupId>org.slf4j</groupId>-->
            <!--<artifactId>slf4j-api</artifactId>-->
          <!--</exclusion>-->
          <!--<exclusion>-->
            <!--<groupId>org.slf4j</groupId>-->
            <!--<artifactId>jcl-over-slf4j</artifactId>-->
          <!--</exclusion>-->
          <!--<exclusion>-->
            <!--<groupId>org.slf4j</groupId>-->
            <!--<artifactId>slf4j-log4j12</artifactId>-->
          <!--</exclusion>-->
          <!--<exclusion>-->
            <!--<groupId>log4j</groupId>-->
            <!--<artifactId>log4j</artifactId>-->
          <!--</exclusion>-->
        <!--</exclusions>-->
      <!--</dependency>-->
      <dependency>
        <groupId>javax.resource</groupId>
        <artifactId>connector-api</artifactId>
        <version>1.5</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>co.kuali</groupId>
        <artifactId>xapool</artifactId>
        <version>${xapool.version}</version>
        <exclusions>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.oracle</groupId>
            <artifactId>ojdbc7</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>commons-beanutils</groupId>
        <artifactId>commons-beanutils</artifactId>
        <version>${commons-beanutils.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.quartz-scheduler</groupId>
        <artifactId>quartz</artifactId>
        <version>${quartz.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-aop</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-beans</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context</artifactId>
        <version>${spring.version}</version>

      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context-support</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-core</artifactId>
        <version>${spring.version}</version>
        <exclusions>
          <exclusion>
            <artifactId>commons-logging</artifactId>
            <groupId>commons-logging</groupId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-jdbc</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-orm</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-test</artifactId>
        <version>${spring.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-tx</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-webmvc</artifactId>
        <version>${spring.version}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-aws</artifactId>
        <version>${spring.cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
          <exclusions>
          <exclusion>
              <groupId>org.slf4j</groupId>
              <artifactId>slf4j-api</artifactId>
          </exclusion>
          </exclusions>
      </dependency>

      <dependency>
        <groupId>com.google.code.findbugs</groupId>
        <artifactId>jsr305</artifactId>
        <version>${jsr305.version}</version>
      </dependency>

      <!-- bitronix -->
      <dependency>
        <groupId>org.codehaus.btm</groupId>
        <artifactId>btm</artifactId>
        <version>${bitronix.version}</version>
        <exclusions>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <!-- Added dependency to improve spring bean loading e.g KualiDefaultListableBeanFactory in rice-core-framework -->
      <dependency>
        <groupId>javax.inject</groupId>
        <artifactId>javax.inject</artifactId>
        <version>${javax.inject.version}</version>
      </dependency>

      <dependency>
        <groupId>org.freemarker</groupId>
        <artifactId>freemarker</artifactId>
        <version>${freemarker.version}</version>
      </dependency>

      <dependency>
        <groupId>org.codehaus.plexus</groupId>
        <artifactId>plexus-utils</artifactId>
        <version>${plexus-utils.version}</version>
      </dependency>

      <dependency>
        <groupId>org.lesscss</groupId>
        <artifactId>lesscss</artifactId>
        <version>${lesscss.version}</version>
        <exclusions>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>com.yahoo.platform.yui</groupId>
        <artifactId>yuicompressor</artifactId>
        <version>${yuicompressor.version}</version>
      </dependency>

      <dependency>
        <groupId>com.google.errorprone</groupId>
        <artifactId>error_prone_annotations</artifactId>
        <version>${error_prone_annotations.version}</version>
      </dependency>

      <dependency>
        <groupId>com.google.javascript</groupId>
        <artifactId>closure-compiler</artifactId>
        <version>${closure.version}</version>
        <exclusions>
          <exclusion>
            <!-- conflicts with guava -->
            <groupId>com.google.errorprone</groupId>
            <artifactId>error_prone_annotations</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <dependency>
        <groupId>org.apache.velocity</groupId>
        <artifactId>velocity-tools</artifactId>
        <version>${velocity.tools.version}</version>
        <exclusions>
          <exclusion>
            <groupId>commons-chain</groupId>
            <artifactId>commons-chain</artifactId>
          </exclusion>
          <exclusion>
            <groupId>commons-digester</groupId>
            <artifactId>commons-digester</artifactId>
          </exclusion>
          <exclusion>
            <groupId>commons-beanutils</groupId>
            <artifactId>commons-beanutils</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts-taglib</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts-tiles</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.struts</groupId>
            <artifactId>struts-core</artifactId>
          </exclusion>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
          <exclusion>
            <groupId>xml-apis</groupId>
            <artifactId>xml-apis</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>org.apache.velocity</groupId>
        <artifactId>velocity</artifactId>
        <version>${velocity.version}</version>
      </dependency>
      <dependency>
        <groupId>org.owasp.encoder</groupId>
        <artifactId>encoder-jsp</artifactId>
        <version>1.2.1</version>
      </dependency>

      <dependency>
        <groupId>org.xmlunit</groupId>
        <artifactId>xmlunit-core</artifactId>
        <version>${xmlunit.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.xmlunit</groupId>
        <artifactId>xmlunit-assertj</artifactId>
        <version>${xmlunit.version}</version>
        <scope>test</scope>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <scm>
    <connection>${scm.connection}</connection>
    <developerConnection>${scm.developerConnection}</developerConnection>
    <url>${scm.url}</url>
    <tag>rice-2.5.3.1504.1-kckualico</tag>
  </scm>

  <repositories>
    <repository>
      <id>kualico.nexus</id>
      <name>KualiCo Nexus Group</name>
      <url>https://nexus.kuali.co/nexus/content/groups/public/</url>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
      <releases>
        <enabled>true</enabled>
      </releases>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>kualico.nexus</id>
      <name>KualiCo Nexus Group</name>
      <url>https://nexus.kuali.co/nexus/content/groups/public/</url>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
      <releases>
        <enabled>true</enabled>
      </releases>
    </pluginRepository>
  </pluginRepositories>


</project>
